export default interface Entry {
    entry_id: string;
    entry: string;
    gramm: string;
    definition: string;
    examples: Array<string>;
    audios: Array<string>;
    images: Array<string>;
    initialLetter: string
}